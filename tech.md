С удовольствием помогу вам составить подробное техническое задание (ТЗ) для создания клона Twitter.

Вот грамотно оформленное ТЗ, учитывающее все ваши требования:

---

# 📝 Техническое Задание: Клон Twitter (Microblogging Platform)

## 1. Общие сведения

*   **Название проекта:** Microblogging Platform (Клон Twitter)
*   **Цель проекта:** Разработка полнофункциональной минимально жизнеспособной версии (MVP) социальной сети для микроблоггинга с функционалом, имитирующим Twitter.
*   **Язык интерфейса:** Английский (English)

---

## 2. Стек технологий

| Категория | Технология | Примечание |
| :--- | :--- | :--- |
| **Backend (API)** | **Django Rest Framework (DRF)** | Для быстрого создания мощного и масштабируемого API. |
| **Frontend** | **Vue.js** (Предпочтительно Vue 3) | Реактивный интерфейс. |
| **База данных** | **PostgreSQL** | Надежная реляционная база данных для структурных данных. |
| **Контейнеризация** | **Docker / Docker Compose** | Для легкой развертки и согласованности сред разработки/продакшена. |

---

## 3. Принципы разработки

*   **ООП (Объектно-Ориентированное Программирование):** Строгое следование принципам ООП (инкапсуляция, наследование, полиморфизм) на стороне бэкенда (Python/DRF).
*   **DRY (Don't Repeat Yourself):** Избегание дублирования кода, использование миксинов, абстрактных классов и переиспользуемых компонентов.
*   **Маппинг данных (Mapping):** Четкое разделение логики и данных, использование сериализаторов (DRF) для маппинга моделей в API-ответы и наоборот.
*   **Архитектура:** Предпочтительно архитектура "Сервис-Репозиторий" или аналогичная для разделения бизнес-логики и доступа к данным.
*   **Тестирование:** Включить базовое модульное тестирование для критически важных компонентов (например, аутентификации, создания поста).

---

## 4. Дизайн и UX

*   **Копирование дизайна:** Дизайн интерфейса (цветовая схема, шрифты, расположение элементов, иконография) должен быть **полностью скопирован** с текущего дизайна Twitter (X).
*   **Адаптивность:** Интерфейс должен быть адаптивным (responsive) и корректно отображаться на мобильных устройствах и десктопе.

---

## 5. Функциональные требования (MVP)

### 5.1. Аутентификация и Профили Пользователей

| ID | Функционал | Backend (API) | Frontend (Vue) |
| :--- | :--- | :--- | :--- |
| 5.1.1 | **Регистрация** | Endpoint для создания нового пользователя (имя, email, пароль). | Форма регистрации. |
| 5.1.2 | **Вход/Выход** | Endpoint для входа (логин/пароль) и получения токена (JWT предпочтительно). | Форма входа, кнопка "Log Out". |
| 5.1.3 | **Профиль пользователя** | Endpoint `GET /api/users/{username}` с данными: имя, юзернейм, био, дата регистрации, количество подписчиков/подписок. | Страница `/profile/{username}` с отображением информации и списка постов пользователя. |
| 5.1.4 | **Подписки (Follow)** | Endpoint `POST /api/users/{id}/follow` для подписки/отписки. | Кнопка "Follow"/"Unfollow" на странице профиля. |

### 5.2. Посты (Твиты)

| ID | Функционал | Backend (API) | Frontend (Vue) |
| :--- | :--- | :--- | :--- |
| 5.2.1 | **Создание поста** | Endpoint `POST /api/posts/` для создания поста (текст, ограничение 280 символов). | Форма для ввода текста с счетчиком символов. |
| 5.2.2 | **Просмотр поста** | Endpoint `GET /api/posts/{id}`. | Отдельная страница поста. |
| 5.2.3 | **Лайки** | Endpoint `POST /api/posts/{id}/like` для добавления/удаления лайка. | Кнопка "Like" с отображением количества. |
| 5.2.4 | **Комментирование** | Endpoint `POST /api/posts/{id}/comment` для добавления ответа/комментария. | Форма ответа под постом, отображение цепочки комментариев. |

### 5.3. Лента Постов (Feed)

| ID | Функционал | Backend (API) | Frontend (Vue) |
| :--- | :--- | :--- | :--- |
| 5.3.1 | **Лента (Home Feed)** | Endpoint `GET /api/feed/` - посты всех пользователей, на которых подписан текущий пользователь, отсортированные по дате. | Главная страница с отображением ленты. |
| 5.3.2 | **Пагинация** | Внедрение пагинации (offset/limit или курсорная) для всех списков постов (лента, профиль). | "Бесконечный скролл" или кнопки "Load More" для подгрузки постов. |

### 5.4. Чат (Прямые сообщения, Direct Messages)

| ID | Функционал | Backend (API) | Frontend (Vue) |
| :--- | :--- | :--- | :--- |
| 5.4.1 | **Создание чата** | Endpoint для начала приватного диалога между двумя пользователями. | Список диалогов, возможность начать новый чат. |
| 5.4.2 | **Отправка сообщения** | Endpoint `POST /api/chats/{id}/messages` (желательно с использованием WebSockets для real-time). | Интерфейс чата с полем ввода. |
| 5.4.3 | **История сообщений** | Endpoint `GET /api/chats/{id}/messages` для получения истории сообщений в диалоге. | Отображение истории сообщений. |

---

## 6. Требования к Docker

*   **Docker Compose:** Должен быть настроен `docker-compose.yml` для запуска всех компонентов одной командой.
    *   Сервис **Backend (DRF)**
    *   Сервис **Frontend (Vue)**
    *   Сервис **PostgreSQL**
    *   (Опционально) Сервис **Redis** для кэширования или WebSockets.
*   **Изоляция:** Каждый компонент должен быть в своем контейнере.
*   **Разработка:** Должна быть обеспечена возможность "горячей перезагрузки" (hot-reloading) для Vue и DRF в режиме разработки.

---

## 7. Критерии приемки

*   Все 4 основные области функциональности (Профили, Посты, Лента, Чат) должны быть реализованы.
*   Стек технологий должен соответствовать заявленному (DRF, Vue, PostgreSQL, Docker).
*   Дизайн интерфейса максимально приближен к Twitter (X).
*   Код соответствует принципам ООП и DRY.

---

Хотите ли вы, чтобы я начал с описания базовой структуры проекта и моделей базы данных, или, возможно, с настройки Docker Compose?